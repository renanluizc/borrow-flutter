name: borrowed_flutter
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  datetime_picker_formfield: ^1.0.0
  flutter_slidable: ^0.5.4
  intl: ^0.16.1
  flushbar: ^1.10.4
  image_picker: ^0.6.7
  path_provider: ^1.6.9
  sembast: ^2.4.4+1

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
